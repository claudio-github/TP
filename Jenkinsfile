pipeline {
    agent any

    stages {
        stage ('Checkout do codigo') {
            steps {
                git url: 'https://github.com/claudio-github/TP.git', branch 'master'
            }
        }
    }
}